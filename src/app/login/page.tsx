import LoginForm from '@/components/Auth/LoginForm'; // Adjust import path if needed

export default function LoginPage() {
    return (
        <div className="min-h-screen flex items-center justify-center">
            <LoginForm />
        </div>
    );
}