import RegisterForm from '@/components/Auth/RegisterForm'; // Adjust import path if needed

export default function RegisterPage() {
    return (
        <div className="min-h-screen flex items-center justify-center">
            <RegisterForm />
        </div>
    );
}