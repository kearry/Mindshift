import CreateTopicForm from '@/components/Topics/CreateTopicForm'; // Adjust import path if needed

export default function CreateTopicPage() {
    return (
        <div>
            {/* Optional: Add page title or breadcrumbs */}
            <CreateTopicForm />
        </div>
    );
}